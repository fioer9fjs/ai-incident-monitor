# AI Incident Monitor — pinned dependencies
# Update via: pip install -r requirements.txt && pip freeze > requirements.txt
# Security audit: pip-audit

PyYAML==6.0.2
google-cloud-bigquery==3.43.0
requests==2.34.2
beautifulsoup4==4.13.3
python-dotenv==1.0.1
google-generativeai==0.8.3
openai==1.60.0

# Dev / CI only (not installed in production ingest)
# bandit==1.7.9
# pip-audit==2.7.3
# pytest==9.1.1


